# 【从权 第 1/9 条 · 权利要求 2】隔离加装

阶段：`从权撰写` 第 3 步。布局表权 2 行已由用户以「下一步」确认。本条只写权 2，不写下一条。

假想代入权 1 后的完整方案：多只保护装置沿机架分布，信号输出端一对一电连接至采集单元的独立输入通道；采集单元含控制器、输入采样电路、存储模块、显示模块，按通道映射显示安装点位及故障类型。本条在该方案上增加与采集单元分设的生产控制装置，并使保护装置信号与采集单元均不进入其启停控制回路，从而监测与生产启停解耦。拼合后可实施，不缺前提，不与权 1 一对一采集矛盾。

---

## 正文

2. 根据权利要求1所述的带式输送机保护装置故障定位系统，其特征在于，还包括生产控制装置；
所述生产控制装置与所述采集单元分设；
所述生产控制装置包括启停控制回路；
各所述保护装置的信号输出端不接入所述启停控制回路；
所述采集单元不接入所述启停控制回路。

---

## 对应布局表行

权 2，引权 1，一级。改进点：采集单元相对生产侧控制装置分设；保护装置信号与采集单元均不接入启停控制回路（交底「独立加装 / 不参与原启停联锁」的产品写法，不写加装步骤）。

---

## 命名（部件命名优化总控）

已入权对照（规则五）：保护装置；采集单元；带式输送机机架；信号输出端；控制器；输入采样电路；存储模块；显示模块；输入通道；安装点位；故障类型；映射关系；电平跳变。

| 拟用名 | 分型 | 两档 | 采用名 | 理由 |
|--------|------|------|--------|------|
| 交底「生产主控 PLC / 原有生产主控系统」 | B+A+H；与权 1「控制器」近义须消歧 | 控制装置 / 生产控制装置 | **生产控制装置** | 不用裸名「控制器」「主控」，避免与采集侧控制器同名多物；去掉 PLC 以免钉死实施例；「生产」保留为功能结构名，用于与采集单元区分 |
| 交底「启停及联锁控制」 | B，回路名为功能结构名 | 控制回路 / 启停控制回路 | **启停控制回路** | 作为生产控制装置的内部构成，不与母件并列；不写「联锁」以免与暂缓的次 4 分级停机缠在一起 |
| 「独立加装」 | — | — | **不入名，改写为「分设」** | 产品权禁止使用过程 |

已调用：`部件命名优化总控` → `交底口语名改写为自明结构名规范`、`部件命名具体优化`（规则五）、`自定义名词就地定义`（名称已自明，未用「其中」）、`结构类权利要求撰写`（母子件）、`功能性限定使用边界规范`（未另加「从而不停产」类效果尾）。

未升格独权级命名对照表（不回改权 1）。

---

## 已遵循 skill

- **全文 Read（A/B）：** `部件命名优化总控`；`交底口语名改写为自明结构名规范`；`部件命名具体优化`；`自定义名词就地定义`；`结构类权利要求撰写`；`权利要求依附编号与术语出处规范`；`功能性限定使用边界规范`（本条未写「用于…以便…」效果尾，已核边界）。
- **摘要自检（C）：** `权利要求主谓宾结构规范`；`权利要求指代与参照清晰性规范`；`权利要求形式合规性规范`；`权利要求清楚性与逻辑一致性规范`（共用内核）。

---

## 微型自检

1. **引用方向：** 引权 1＜2；主题名称与独权一致（带式输送机保护装置故障定位系统）；非多项从权。通过。
2. **术语出处：** 见下表。无覆盖失败。
3. **结构类：** 本层总列仅「生产控制装置」；展开为与采集单元分设，并再总列其子件「启停控制回路」；启停控制回路以「不接入」落地连接关系。已逐件展开：是。未写使用步骤。未把启停控制回路与生产控制装置、采集单元写成同级「还包括」。
4. **命名：** 见上；无跨权同名多物未消歧。
5. **C 层五勾：**
   - 主谓宾：分设、包括、不接入均有主语（生产控制装置 / 信号输出端 / 采集单元）。勾。
   - 指代：无「该/其」；「所述」均有唯一对象。勾。
   - 方位参照：无周向/轴向等。勾。
   - 形式微核：见第 1 点。勾。
   - 清楚性：无孤岛；启停控制回路有连接关系；无模糊程度词；与权 1 一对一接采集单元不矛盾。勾。
6. **假想代入：** 已代入权 1 全文，见文首。勾。

---

## 术语出处微表

| 所述词 | 首次出现权号 | 本条引用链是否覆盖 | 处置 |
|--------|--------------|--------------------|------|
| 带式输送机保护装置故障定位系统（引用套话） | 1 | 是（2→1） | 通过（主题套话可不入表，已核） |
| 生产控制装置 | 本条新引入（还包括） | — | 通过 |
| 采集单元 | 1 | 是 | 通过 |
| 启停控制回路 | 本条新引入（所述生产控制装置包括） | — | 通过 |
| 保护装置 | 1 | 是 | 通过 |
| 信号输出端 | 1 | 是 | 通过 |

引用链：2 → 1。最大须出处号 = 1，已覆盖。

---

## 请确认

请确认权 2 后写权 3（输入通道为数字量开关量通道，引权 1）。

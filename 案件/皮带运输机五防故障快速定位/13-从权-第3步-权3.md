# 【从权 第 2/9 条 · 权利要求 3】数字量 / 开关量通道

阶段：`从权撰写` 第 3 步。权 2 已由用户以「下一步」确认。本条只写权 3，不写权 4。引用权 1，不引用权 2（与隔离平行）。

假想代入权 1 后的完整方案：多只保护装置沿机架分布，信号输出端一对一电连接至采集单元相互独立的输入通道；采集单元含控制器、输入采样电路、存储模块、显示模块，按产生电平跳变的通道调取映射并显示安装点位及故障类型。本条把信号输出端限定为开关量输出端、把输入通道限定为数字量通道，与权 1 的电平跳变相合，排除仅模拟量通道误读五防触点。拼合后可实施，不引入权 2 的生产控制装置，不缺前提。

---

## 正文

3. 根据权利要求1所述的带式输送机保护装置故障定位系统，其特征在于，各所述信号输出端为开关量输出端，各所述输入通道为数字量通道。

---

## 对应布局表行

权 3，引权 1，一级，主·主干。改进点：输入通道落实为数字量通道；信号输出端同步限定为开关量，对应交底「独占数字量输入通道 / 开关量传感器」，不另起部件、不钉 PNP 或 DI 点数。

---

## 命名（部件命名优化总控）

已入权对照（含权 2，供规则五）：保护装置；采集单元；带式输送机机架；信号输出端；控制器；输入采样电路；存储模块；显示模块；输入通道；安装点位；故障类型；映射关系；电平跳变；生产控制装置；启停控制回路。

| 拟用名 | 分型 | 两档 | 采用名 | 理由 |
|--------|------|------|--------|------|
| 交底「数字量输入通道」叠「开关量」 | A（类型定语，本条必要） | 数字量通道 / 开关量通道 | **对已有「输入通道」写成数字量通道；对已有「信号输出端」写成开关量输出端** | 不新造「数字量开关量通道」以免同义叠词、也不另列部件；两侧分别落在已有端与通道上。不用裸名「通道」以免与输入通道同名多物 |
| DI / PNP / 8 路 | A 实施例钉死 | — | **不入权** | 暂缓牌号与点数 |

本条无新母件。已调用：`部件命名优化总控` → `部件命名具体优化`（规则五：属性限定，不另起裸名）。口语改写：数字量 / 开关量为工控公知，不改。未用「其中」。未命中方位。结构类总列：不适用。

---

## 已遵循 skill

- **全文 Read（A）：** `部件命名优化总控`（本会话已读，规则未改）；`部件命名具体优化`（本会话已读）；`权利要求依附编号与术语出处规范`（本会话已读）。结构类：本条无「还包括」、无新内部构成，未再展开总列。
- **条件 B：** 未写「用于…以便…」；无方位/场景钉死；无自定义黑话；无预设阈值。未触发方法细则。
- **摘要自检（C）：** `权利要求主谓宾结构规范`；`权利要求指代与参照清晰性规范`；`权利要求形式合规性规范`；`权利要求清楚性与逻辑一致性规范`。

---

## 微型自检

1. **引用方向：** 引权 1＜3；主题名称一致；非多项。通过。
2. **术语出处：** 见下表。无覆盖失败。未使用权 2 才引入的「生产控制装置 / 启停控制回路」。
3. **结构类：** 已逐件展开：不适用。
4. **命名：** 见上；无新裸名与已入权名撞车。
5. **C 层五勾：**
   - 主谓宾：「为」的主语是信号输出端、输入通道。勾。
   - 指代：无「该/其」。勾。
   - 方位：无。勾。
   - 形式：见第 1 点。勾。
   - 清楚性：无孤岛；数字量 / 开关量为公知类型词；与权 1 电平跳变不矛盾。勾。
6. **假想代入：** 已代入权 1 全文，见文首。勾。

---

## 术语出处微表

| 所述词 | 首次出现权号 | 本条引用链是否覆盖 | 处置 |
|--------|--------------|--------------------|------|
| 带式输送机保护装置故障定位系统（引用套话） | 1 | 是（3→1） | 通过 |
| 信号输出端 | 1 | 是 | 通过 |
| 输入通道 | 1 | 是 | 通过 |

引用链：3 → 1。最大须出处号 = 1，已覆盖。

「开关量输出端」「数字量通道」为本条对已有端 / 通道的类型限定，不是带「所述」的新部件首次引入。

---

## 请确认

请确认权 3 后写权 4（通道消抖 / 边缘预处理，引权 3）。

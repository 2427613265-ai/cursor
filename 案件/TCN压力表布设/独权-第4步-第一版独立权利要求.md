# 【第4步 / 共7步】最小技术特征集 → 第一版独立权利要求

- 案件：一种基于TCN特征簇与动态增量优化的供水管网压力表布设方法
- 阶段：阶段二 · 方法类独权撰写
- 锚定：第1步交底声称问题（用户已确认）
- 核心区别：第3步四条不变量（用户以「下一步」确认）
- 命名：**本版沿用交底原名，未优化**（不交付命名对照表）
- 日期：2026-08-13

标注：**第一版独权（交底声称问题 · 最小特征集 · 原名未优化）**

写正文前已按 `方法类权利要求具体撰写规则总控` 加载：按产物拆步与IO咬合、方法步骤编号、路径与不变量二分、方法独权成组下放、必要技术特征检查、自定义名词就地定义、条件判断语句、预设值来源、主谓宾、指代、清楚性内核。本案非调控闭环，未挂 `调控闭环最低链`。未触发命名总控。

---

## 阶段二 plan（2.1～2.7）

| 步 | 内容 | 状态 |
|----|------|------|
| 2.1 | 交底声称技术问题 | **已确认** |
| 2.2 | 解题技术特征清单 | **已确认** |
| 2.3 | 核心区别技术特征 | **已确认** |
| 2.4 | 最小特征集 → 第一版独权 | **本步交付** |
| 2.5 | 按第一版重新核定技术问题 | 待开始（须本步全文已交付） |
| 2.6 | 第二版独权 | 待开始 |
| 2.7 | 收束与交接 | 待开始 |

---

## 本步锚定的技术问题（第1步，不抢第5步）

在构建基于TCN端到端漏损定位的供水管网监测系统时，现有压力表布设方案与模型需求脱节、工程实用性差；具体包括：基于静态水力特性的布点无法适配TCN非线性特征提取需求，以及全局增量搜索计算量大、难以用于城市级复杂管网。

---

## 最小技术特征集构成

### 核心区别（第3步四条，必须进入）

| 编号 | 不变量 | 落入主步 |
|------|--------|----------|
| 核1 | 从全节点端到端漏损定位基准模型提取节点级内部特征 | S2 |
| 核2 | 按内部特征相似性划分特征簇（同簇信息冗余） | S3 |
| 核3 | 贡献高且当前无表覆盖 → 信息盲区；候选取自盲区内部 | S4 |
| 核4 | 盲区约束下以定位误差减少量为节点贡献度，盲区级—节点级增量 | S5 |

### 必要支撑（去掉则核心接不上或无法实施）

| 支撑 | 反问：去掉会怎样 | 结论 |
|------|------------------|------|
| 获得目标管网、初始布设、全节点漏损—压力样本 | 无处训练全节点基准模型，也无法判断「当前是否覆盖」 | **留**；获得方式（EPANET 等）不留 |
| 训练全节点TCN漏损定位基准模型 | 核1无特征来源 | **留**（模型种类名沿用交底 TCN；层数/结构不留） |
| 预先设定表数上限与定位精度要求 | 增量循环无终止，方案不完整 | **留**（数值不留） |
| 每加入一点后重新计算剩余节点定位误差贡献度 | 无此则选点序列不是交底的「动态」增量，后点贡献不随已选集合变化 | **留计算更新**；不留「每步重新训练TCN」这一评价路径 |
| 输出优化后的压力表布设方案 | 方法无产物 | **并入 S5 产物** |

### 明确不进最小集（能去掉仍能对声称问题做功）

A1 适用范围、A6/A7 初始精度门控、A3/A4 仿真工具与时段参数、B2 余弦矩阵、谱聚类/簇数/维数/末层、B4 具体误差公式、C4/C6 切换公式、C7 重训TCN、C8 冗余检测、D1/D2 独立验证集复核。

---

## 步骤编号对照（S1…S5）

| 编号 | 本步产物 | 输入来自 |
|------|----------|----------|
| S1 | 目标供水管网、初始压力表布设方案、全节点仿真数据集；以及预设的压力表数量上限、定位精度要求 | 非区别数据：获得 |
| S2 | 全节点TCN漏损定位基准模型；各节点高维特征向量 | S1 的全节点仿真数据集 |
| S3 | 特征簇 | S2 的高维特征向量 |
| S4 | 信息盲区；候选节点池 | S3 的特征簇 + S1 的初始布设 + 各簇贡献程度（本步算得） |
| S5 | 优化后的压力表布设方案 | S1 初始布设与两预设；S4 信息盲区与候选池；定位误差贡献度（本步算得并更新） |

主步 5 个，按产物切分；增量循环写在 S5 一步内，未按加表轮次拆碎。

---

## 第一版独立权利要求

**第一版独权（交底声称问题 · 最小特征集 · 原名未优化）**

1. 一种基于TCN特征簇与动态增量优化的供水管网压力表布设方法，其特征在于，包括以下步骤：

S1：获得目标供水管网、所述目标供水管网的初始压力表布设方案，以及由全管网各节点的压力时序与漏损坐标绑定得到的全节点仿真数据集；预先设定压力表数量上限和定位精度要求；

S2：以所述全节点仿真数据集中所有节点的压力时序为输入、漏损坐标为输出，训练全节点TCN漏损定位基准模型，提取所述全节点TCN漏损定位基准模型输出的各节点的高维特征向量，所述高维特征向量表征相应节点在漏损事件中的非线性时空响应模式；

S3：根据各节点的所述高维特征向量的相似性，将全管网节点划分为若干特征簇，同一特征簇内的节点对TCN端到端漏损定位的信息贡献存在冗余；

S4：确定各特征簇对TCN端到端漏损定位的贡献程度，将所述贡献程度高且在所述初始压力表布设方案中缺乏压力表覆盖的特征簇作为信息盲区，由所述信息盲区内的节点构成候选节点池；

S5：以所述初始压力表布设方案为当前布设集合，计算所述候选节点池中各候选节点的定位误差贡献度，所述定位误差贡献度为将该候选节点加入所述当前布设集合后全网平均定位误差的减少量；判断是否达到所述压力表数量上限或所述定位精度要求，若否，则在尚未被压力表覆盖的信息盲区中按各信息盲区对应的所述贡献程度确定当前优先的信息盲区，在所述当前优先的信息盲区内选取定位误差贡献度最高的候选节点加入所述当前布设集合，并重新计算剩余候选节点的所述定位误差贡献度，再次进行本步骤所述判断；若是，则将所述当前布设集合作为优化后的压力表布设方案。

---

## 正文自检（本版）

| 项 | 结果 |
|----|------|
| 核心四条均在 | 核1=S2，核2=S3，核3=S4，核4=S5 |
| 路径未灌入 | 无 EPANET、谱聚类、余弦、末层、128维、K、冗余检测、节点编号 |
| 命名 | 全节点TCN漏损定位基准模型、高维特征向量、特征簇、信息盲区、定位误差贡献度、候选节点池均为交底原名 |
| 自定义名 | 特征簇、信息盲区、定位误差贡献度均在短名前用白话引入，未堆「其中」 |
| 条件句 | S5 用「判断是否……，若否，则……；若是，则……」 |
| 预设来源 | 压力表数量上限、定位精度要求在 S1 预先设定，S5 再使用 |
| 空挂 | S1 三项数据与两预设均在后续被消费 |
| 指代 | 用「所述」；「当前优先的信息盲区」与「当前布设集合」已消歧 |
| 未做第5步 | 未重新核定问题，未写第二版 |

---

## 成组下放候选（按 S1…S5，供从权种子，本步不写从权全文）

| 挂靠 | 下放特征摘要 | 建议从权主题 |
|------|--------------|--------------|
| S1 | EPANET/水力数字孪生、漏损面积与时段参数、24小时、样本规模；既支持现有方案增量也支持从零布设；初始精度评估与未达阈值才优化（A6/A7） | 仿真造数方式；入口评估门控 |
| S2 | 残差块数/多尺度结构、最后一个卷积层、128维；替换为 LSTM/Transformer/GNN 等能输出节点级高维特征的端到端模型 | 基准模型结构与取特征位置；模型种类扩展 |
| S3 | 余弦相似度矩阵、谱聚类、特征值法定簇数、实施例7簇 | 分簇算法与簇数 |
| S4 | 「仅以该簇训练的平均误差」或「缺失该簇传感器后误差升高」；实施例两盲区、196→32 | 簇贡献度量；盲区数量与压缩比 |
| S5 | 每步重新训练TCN；边际贡献衰减率/感知充分系数；至少一个表覆盖即降优先级；每K点冗余检测；独立测试集复核与安装优先级清单 | 贡献度更新方式；盲区切换细则；冗余回溯；验证输出 |

区别骨架（核1～核4）未整组下放。

---

## 请确认后进入第5步

请确认：第一版独权是否把第3步四条核心区别写全、有无把实施例路径（谱聚类、末层、冗余检测等）误灌进去、有无缺环。

确认后进入第5步：仅根据**本步这一份第一版独权全文**重新核定技术问题（服务扩围，不写第二版正文）。

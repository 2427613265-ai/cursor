# 【第6步 / 共7步】6a 命名对照表（确认前不写第二版正文）

- 案件：一种基于TCN特征簇与动态增量优化的供水管网压力表布设方法
- 阶段：阶段二 · 方法类独权撰写 · **本案唯一一次命名优化**
- 锚定：第5步已确认的重新核定技术问题
- 日期：2026-08-13

**待确认：独权部件命名表。确认或改选采用名之前，不写第二版独权正文。**

已挂接：`方法独权命名时机` → `部件命名优化总控` → `部件命名具体优化`、`交底口语名改写为自明结构名规范`、`方位与场景限定上位规范`、`自定义名词就地定义`（同名多物消歧回具体优化规则五）。

---

## 阶段二 plan（2.1～2.7）

| 步 | 内容 | 状态 |
|----|------|------|
| 2.1～2.5 | 至重新核定技术问题 | **已确认** |
| 2.6a | 命名对照表【须确认采用名】 | **本步交付，待确认** |
| 2.6b | 第二版独权正文 | 待命名确认后 |
| 2.7 | 收束与交接 | 待开始 |

---

## 新问题下第一版何处过窄（命名层预告，正文留 6b）

核定问题已去掉 TCN、供水管网、城市级、仿真钉死。第一版主题与 S1/S2 仍写死这些词，相对新问题**过窄**。本表先定采用名；6b 再按采用名删减/上位步骤表述。

区别骨架名（特征簇、信息盲区、定位误差贡献度）改不动则保留短名，靠前置白话说清，不改成空泛「智能分区」。

---

## 命名对照表

关键入权对象与中间量逐一入表；关键名两档。用户选择列均为待确认。

| 原名（交底/第一版） | 分型 | 第一档优化 | 第二档优化 | 推荐采用名 | 对保护范围的影响 | 用户选择 |
|---------------------|------|------------|------------|------------|------------------|----------|
| 一种基于TCN特征簇与动态增量优化的供水管网压力表布设方法 | A+C 手段堆砌+场景 | 一种管网压力监测点布设方法 | 一种面向漏损定位的管网压力监测点布设方法 | **一种面向漏损定位的管网压力监测点布设方法** | 去掉 TCN/特征簇/动态增量/供水钉死，仍钉在漏损定位任务上，避免假宽成任意布点 | 待确认 |
| 目标供水管网 / 供水管网 | C 场景 | 管网 | 供水管网 | **管网** | 不限饮用水供水；机制只用节点、压力时序、漏损位置 | 待确认 |
| 压力表 | A 器具钉死 | 压力监测点 | 压力表 | **压力监测点** | 覆盖传感器/变送器等，不限机械压力表 | 待确认 |
| 初始压力表布设方案 | 随上条 | 初始压力监测点布设方案 | 初始压力表布设方案 | **初始压力监测点布设方案** | 与「压力监测点」全书一致 | 待确认 |
| 全节点仿真数据集 | A 获得路径 | 全节点压力时序及对应漏损位置 | 全节点仿真数据集 | **全节点压力时序及对应漏损位置** | 不限仿真；实测/数字孪生均可「获得」 | 待确认 |
| 漏损坐标 | A 输出形式 | 漏损位置 | 漏损坐标 | **漏损位置** | 不限绝对 XY 坐标回归 | 待确认 |
| 全节点TCN漏损定位基准模型 | A+D 算法钉死 | 全节点端到端漏损定位基准模型 | 全节点漏损定位基准模型 | **全节点端到端漏损定位基准模型** | 去掉 TCN；保留端到端（对准核定问题）。LSTM 等从权收回 | 待确认 |
| 高维特征向量 | A 维数定语 | 节点特征向量 | 节点级内部特征 | **节点特征向量** | 去掉「高维」钉死；用前置白话写清表征非线性响应 | 待确认 |
| 特征簇 | D 本案特有 | 特征相似节点组 | 特征簇（前置白话） | **特征簇**（前置白话，不改名） | 改成「组/集合」并不更自明，且抽空交底核心标签；靠引入句说清 | 待确认 |
| 信息盲区 | D 本案特有 | 待覆盖的高贡献特征簇 | 信息盲区（前置白话） | **信息盲区**（前置白话，不改名） | 后文多次回指需要短名；含义按「贡献高且当前无监测点」引入 | 待确认 |
| 贡献程度（簇级）与定位误差贡献度（节点级） | E 同名易混 | 特征簇贡献程度 / 节点定位误差贡献度 | 贡献程度 / 定位误差贡献度（第一版原样） | **特征簇贡献程度**；**节点定位误差贡献度** | 加归属前缀消歧，避免「所述贡献程度」双指 | 待确认 |
| 候选节点池 | B 口语「池」 | 候选节点集合 | 候选节点池 | **候选节点集合** | 更中性，范围不变 | 待确认 |
| 当前布设集合 | 随压力监测点 | 当前压力监测点集合 | 当前布设集合 | **当前压力监测点集合** | 与监测点术语对齐 | 待确认 |
| 压力表数量上限 | 随压力监测点 | 压力监测点数量上限 | 压力表数量上限 | **压力监测点数量上限** | 随监测点改名，非扩围关键 | 待确认 |

次要名（一档即可）：**节点、压力时序、全网平均定位误差、定位精度要求** 保留原名（领域公知或已自明）。

---

## 分型与调用摘要

| 分型 | 命中项 | 已调用 |
|------|--------|--------|
| A 非必要定语/工具名 | TCN、仿真、高维、压力表器具 | 部件命名具体优化 |
| B 口语 | 候选节点池 | 交底口语名改写 |
| C 场景 | 供水管网 | 方位与场景限定上位 |
| D 改不动特有词 | 特征簇、信息盲区 | 自定义名词就地定义（6b 前置白话） |
| E 同名易混 | 两级「贡献」 | 部件命名具体优化规则五 |

未走结构母子件层级；名称中的「端到端」是任务形态不是偷带效果，保留在基准模型名中以对准核定问题。

---

## 推荐采用名一览（若全部按推荐）

主题：**一种面向漏损定位的管网压力监测点布设方法**

管网；压力监测点；初始压力监测点布设方案；全节点压力时序及对应漏损位置；漏损位置；全节点端到端漏损定位基准模型；节点特征向量；特征簇；信息盲区；特征簇贡献程度；节点定位误差贡献度；候选节点集合；当前压力监测点集合；压力监测点数量上限。

---

## 待确认：独权部件命名表

请确认或改选各行采用名（可只回复「按推荐」或指出要改的行）。确认后进入 6b 写第二版独权正文。

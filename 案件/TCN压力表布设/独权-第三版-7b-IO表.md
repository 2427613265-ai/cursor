# 第7步·7b：第三版 I/O 对照表 + 路径/不变量再二分（待确认）

**锚定问题：** 沿用第二版（第6步已跳过）。  
**命名：** 按 7a 推荐（漏损定位模型、节点特征表示、压力数据、增量选点等）。  
**正式轨A/B：** 本步仅预列；正文暂按轨A。

---

## 待确认：方法独权 I/O 对照表（第三版）

| 步骤 | 输入 | 输出 | 本步不变量（须留） | 拟下放路径（成组） |
|------|------|------|--------------------|--------------------|
| **S1** | 目标供水管网；漏损相关数据条件；初始监测点集合 | 全节点基准定位模型 | 得到以**全部管网节点压力数据**为支撑的漏损定位基准模型，供后续感知分析 | 仿真工具与工况采样；模型结构（含TCN等）；训练超参；「端到端」实现细节 |
| **S2** | 全节点基准定位模型；管网节点；当前监测点覆盖情况 | 特征簇；信息盲区；候选监测点集合 | 提取节点特征表示并按**相似度**划分特征簇；将**定位贡献高且缺乏监测点覆盖**的特征簇作为信息盲区；由此形成候选监测点集合以收缩搜索空间 | 特征维数/提取层；具体相似度与聚类算法；簇贡献测算实现变体 |
| **S3** | 初始监测点集合；信息盲区；候选监测点集合 | 优化后的监测点集合 | 在信息盲区约束下按**定位误差减少量**增量选点；增补后重评剩余候选；以预设数量上限或预设精度要求终止 | 盲区排序/切换细则；冗余检测；阈值取值；实施例加表轨迹 |
| **S4** | 优化后的监测点集合 | 布设方案（位置及数量） | 输出可实施的监测点布设方案 | 验证流程；优先级；输出格式 |

### 咬合
S1→S2→S3→S4 闭合；用尽无空挂。

---

## 相对第二版的扩围（表达层，不抽空骨架）

| 第二版用语 | 第三版用语 | 说明 |
|------------|------------|------|
| 端到端漏损定位模型 | 漏损定位模型 | 去任务形态钉死 |
| 节点特征向量 / 压力时序 | 节点特征表示 / 压力数据 | 去形态钉死 |
| 动态增量优化 | 增量选点（步骤叙述） | 更中性 |
| 通过…仿真与…训练（概括层） | 仍暂按轨A保留 | 第8步再择轨A/B |

### 不变量再确认（不得下放）
分簇准则、信息盲区定义（高贡献且缺覆盖）、盲区约束选点、按定位误差减少量选取、加点重评、终止条件 → **仍为独权骨架**。

### 轨A/B 预列
- **轨A：** S1「通过多漏损工况仿真与漏损定位模型训练，得到全节点基准定位模型」  
- **轨B：** S1「基于供水管网与管网节点压力数据，得到全节点基准定位模型」  

---

**第三版硬性：** 请确认本表。确认前不得输出第三版独权全文。  
回复「下一步」/「按此表」进入 **7正文**。

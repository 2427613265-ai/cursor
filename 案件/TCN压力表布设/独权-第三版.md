# 第三版独权（清楚且范围最大）· 暂定轨A

**锚定技术问题（沿用第二版，第6步已跳过）：**  
如何使供水管网压力监测点的布设与端到端漏损定位模型的感知需求相匹配，以避免因布点与定位脱节导致定位精度不足，并避免因在全网节点上盲目试探选点而导致布设优化难以实施。

说明：问题表述中的「端到端」保留为场景描述；独权正文已扩为「漏损定位模型」，不以端到端实现钉死保护范围。

**标注：** 第三版独权（清楚且范围最大 · 暂定轨A，第8步统一择轨）  
**依据：** 7a 命名、7b I/O（按推荐）

---

## 权利要求 1

一种供水管网压力监测点布设方法，其特征在于，包括以下步骤：

S1、基于目标供水管网、漏损相关数据条件以及初始监测点集合，通过多漏损工况仿真与漏损定位模型训练，得到以全部管网节点压力数据为输入、以漏损位置为输出的全节点基准定位模型；

S2、基于所述全节点基准定位模型，提取表征各管网节点漏损响应的节点特征表示，并依据所述节点特征表示之间的相似度将管网节点划分为若干特征簇；将定位贡献高且当前缺乏压力监测点覆盖的特征簇作为信息盲区，并由所述信息盲区内的管网节点形成候选监测点集合；

S3、以所述初始监测点集合为当前监测点集合，在所述信息盲区约束下对所述候选监测点集合进行增量选点：在待覆盖的信息盲区内，依据将候选监测点加入当前监测点集合后定位误差的减少量选取监测点加入当前监测点集合，并在加入后重新评估剩余候选监测点对应的定位误差减少量；判断是否达到预设的压力监测点数量上限或定位精度是否满足预设要求，若是，则得到优化后的监测点集合；若否，则继续执行上述增补；

S4、输出包含压力监测点位置及数量的布设方案。

---

## 相对第二版优化对照

| 第二版 | 第三版 | 效果 |
|--------|--------|------|
| 端到端漏损定位模型 | 漏损定位模型 | 去实现形态钉死 |
| 压力时序 / 节点特征向量 | 压力数据 / 节点特征表示 | 略宽 |
| 动态增量优化 | 增量选点 | 叙述中性 |
| 分簇→盲区→约束选点+重评骨架 | 维持 | 不抽空区别 |

## 下放组（维持并微调）

- S11… 仿真工具、工况、模型结构（含原TCN实施例）、训练设置  
- S21… 特征提取实现、相似度与聚类算法  
- S31… 盲区排序/切换、冗余检测、阈值  
- S41… 验证、优先级、输出格式  

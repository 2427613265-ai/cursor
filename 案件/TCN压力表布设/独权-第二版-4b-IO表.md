# 第4步·4b：第二版 I/O 对照表 + 路径/不变量二分（待确认）

**锚定问题：** 监测点布设与端到端漏损定位模型感知需求匹配；避免全网盲目试探导致优化难实施。  
**命名：** 按 4a 推荐。  
**正式轨A/B：** 本步仅预列示意，不择轨；正文将暂按轨A。

---

## 待确认：方法独权 I/O 对照表（第二版）

| 步骤 | 输入 | 输出 | 本步不变量（须留独权） | 拟下放路径（成组） |
|------|------|------|------------------------|--------------------|
| **S1** | 目标供水管网；漏损相关工况/数据条件；初始监测点集合（可为空或已有） | 全节点基准定位模型 | 得到以**全部管网节点**压力信息为支撑、用于漏损定位的基准模型，作为后续感知分析的参照 | EPANET；漏损面积/时段采样；TCN结构与训练超参；损失函数；「初评是否达标」完整分支工序 |
| **S2** | 全节点基准定位模型；管网节点；当前监测点覆盖情况 | 特征簇；信息盲区；候选监测点集合 | ①提取表征节点漏损响应的节点特征向量并按**相似度**划分为特征簇；②将**定位贡献高且缺乏监测点覆盖**的特征簇作为信息盲区；③由信息盲区形成候选监测点集合（相对全网收缩搜索空间） | 最后卷积层/维数；余弦公式；谱聚类及簇数确定；簇贡献度具体测算口径（只训该簇/缺失该簇等实现变体） |
| **S3** | 初始监测点集合；信息盲区；候选监测点集合 | 优化后的监测点集合 | 在**信息盲区约束**下，按加入后**定位误差减少量**选取监测点并增量更新；每增补后**重评**剩余候选；以预设数量上限或预设精度要求为终止条件 | 盲区优先级排序细则；双层循环伪代码；冗余检测（每K点试删）；衰减率/感知充分系数；单点加入的实施例轨迹 |
| **S4** | 优化后的监测点集合 | 压力监测点布设方案（至少含位置与数量） | 输出可实施的布设方案 | 独立测试集验证流程；鲁棒性指标；布设优先级；施工图表达 |

### 咬合
S1→S2→S3→S4 产物链闭合；无空挂输入。

---

## 路径 / 不变量二分说明（规则四）

| 片段 | 归类 | 判问要点 |
|------|------|----------|
| 用EPANET/具体漏损参数造数 | 路径① | 换等价仿真仍可得到基准模型 |
| 用TCN/某层向量提取特征 | 路径① | 换可输出节点级特征的定位模型，I/O仍可成立 |
| 余弦相似度、谱聚类 | 路径① | 「按相似度分簇」为不变量；具体相似度与聚类算法为路径 |
| 按相似度划特征簇 | **不变量③** | 解决感知结构识别；删则难支撑「匹配模型感知」 |
| 高贡献且缺覆盖 → 信息盲区 → 缩候选 | **不变量③** | 直接对应「匹配缺口」+「避免全网试探」 |
| 盲区内按定位误差减少量选点 + 加点重评 | **不变量③** | 区别于静态水力/经验布点与无约束全局搜索 |
| 「通过仿真与训练得到基准模型」 | 概括层（轨A可留/轨B可放） | 细节路径两轨均下放 |
| S4独立重训验收 | 多属路径/辅层 | 第二版主文只保留「输出方案」；验证下从权 |

### 轨A/B 预列（不择轨）
- **轨A（正文暂用）：** S1 可写「通过…仿真与定位模型训练，得到全节点基准定位模型」  
- **轨B：** S1 写「基于供水管网与节点压力时序，得到全节点基准定位模型」；方式进从权 S11…

### 相对第一版 I/O 的收紧
- 弱化/下放：初评完整分支、S4重验证、优先级  
- 加硬：S2/S3 不变量表述与「匹配 + 可实施」显式对齐  

---

**第二版硬性：** 请确认或改表。确认前不得输出第二版独权全文。  
回复「按此表」/「下一步」后，先做必要特征检查要点，再写 **4正文**。

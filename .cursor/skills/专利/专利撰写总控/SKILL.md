---
name: 专利撰写总控
description: 专利撰写顶层入口。用户说「写案子/写专利/从交底书开始」等未点名阶段时先触发本 skill，再分流至独权、从权、说明书或创造性答复。与质检总控对称；不替代各阶段策略 skill。
metadata:
  中文名称: 专利撰写总控
  所属分支: 专利
---

# 专利撰写总控（顶层：只挂阶段入口）

## 适用场景

- 用户说「写案子」「写专利」「从交底开始」「帮我做这份申请」且**未点名**具体阶段；
- 需要与 `质检总控` 对称的撰写侧总入口，避免误触质检叶节点或创造性旧名。

**硬性规则：** 本 skill **只分流**，不产出权要/说明书/答复正文。已点名阶段（如「写从权」「写独权第三版」「答复创造性」）可直接进对应阶段 skill；若误进本 skill，立刻改分流。

**不适用：** 「质检/审核/检查」→ `质检总控`；仅讨论单条规范细节且用户已点名规范名时，可直读该规范。

---

## 分流表

| 类型 | 典型说法 | 下一步 |
|------|----------|--------|
| A. 独权（默认起点） | 「拿到交底书」「写案子」「写独权」「三版独权」「重新核定技术问题」 | `独权撰写`（再分流至 `结构类独权撰写` 等） |
| B. 从权 | 「写从权」「九条从权」「从权布局」「次要权利要求组」「产业链原则」 | `从权撰写` |
| C. 说明书 | 「写说明书」「写具体实施方式」「写背景/摘要」 | `权利要求定稿后说明书撰写`（再挂各说明书子 skill） |
| D. 创造性答复 | 「答复创造性」「写意见陈述」「一通/二通答复」 | `创造性答复总控` |
| E. 对象不明 | 只说「写专利」且无交底/无阶段 | 询问是否已有交底；有则 A，否则请用户补充材料 |

## 推荐全案链路

```text
专利撰写总控
  → 独权撰写（分流）
       → 结构案：`结构类独权撰写`（第1步经 `核心区别技术特征确定`；第2/3步经 `最小技术特征集与第一版独权`；第4步经 `审定技术主题与重新核定技术问题`；第5步经 `第二版独权校核`；至第二版后保留再上位→第三版→可选变劣）
       → 方法/算法案：`方法类独权撰写`（第1～5步同上；至第二版收束）
            → 写正文时：`方法类权利要求具体撰写规则总控`（`方法类权利要求专有细则` + 横切）
  → 从权撰写（核心组从权分层次落实关键创新点；完成后按产业链/对应/单边判定次要组，见 `从权分层次布置` / `次要权利要求组撰写`）
  → 权利要求定稿后说明书撰写
  →（授权前 OA）创造性答复总控
  →（定稿后）质检总控
```

## 与质检总控的关系

| | 撰写 | 质检 |
|--|------|------|
| 顶层 | **本 skill** | `质检总控` |
| 权要二级 | `独权撰写`（入口）/ `结构类独权撰写` / `方法类独权撰写` / `核心区别技术特征确定`（第1步细则）/ `最小技术特征集与第一版独权`（第2/3步细则）/ `审定技术主题与重新核定技术问题`（第4步细则）/ `第二版独权校核`（第5步细则）/ `方法类权利要求具体撰写规则总控`（挂 `方法类权利要求专有细则`）/ `从权撰写` / `从权分层次布置` / `次要权利要求组撰写` 等 | `权利要求质检总控` |
| 说明书二级 | `权利要求定稿后说明书撰写` | `说明书质检总控` |
| 创造性 OA | `创造性答复总控` | （论证在答复链路，不走质检总控） |

## 最低交付

分流时输出：当前判定类型（A～E）+ 将调用的阶段 skill 名 + 一句前置条件（如「须已有定稿权1」）。禁止在本 skill 内展开该阶段全文步骤。

## 检查清单

- [ ] 用户意图是撰写而非质检  
- [ ] 已选对阶段入口，未在本页写正文  
- [ ] 创造性答复走 `创造性答复总控`，非旧名跳转页倾倒  

# 方法类·权利要求撰写

## 分工

| skill | 管什么 |
|-------|--------|
| `方法类独权撰写` | 两版策略与 plan（经 `独权撰写` 分流进入）：声称问题→特征→核心区别→最小集第一版→根据该独权重新核定→第二版 |
| **`方法类权利要求具体撰写规则总控`** | 独权**正文**具体写法入口：挂接方法专有叶 + 通用横切 + 结构侧可复用 |

写某一版独权正文前，经 **`方法类权利要求具体撰写规则总控`** 按表 Read 细则；不要在策略 skill 里展开叶规则全文。

## 方法专有叶（由总控挂接）

- `按产物拆步与IO咬合`（已建）
- `方法步骤编号规范`（已建）
- `路径与不变量二分`（已建）
- `调控闭环最低链`（已建）
- `方法独权成组下放`（已建）
- `方法独权命名时机`（已建）

## 通用横切（示例）

- `条件判断语句规范`
- `预设值与参数来源检查`
- `权利要求主谓宾结构规范`
- `自定义名词就地定义`

**禁止**再把方法案套进 `结构类独权撰写` / `结构类权利要求撰写` 的「先总列后展开」。

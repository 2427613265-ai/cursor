---
name: 方法类权利要求撰写
description: 撰写或审核方法类（含算法流程型）权利要求时使用。管分步I/O、步内用尽、路径/不变量三问与可实施下限及扩围；由独权撰写挂接。不管三版策略与条件句/预设值等横切规范。
metadata:
  中文名称: 方法类权利要求撰写
  所属分支: 专利/专利撰写/方法类/权利要求撰写
---

# 方法类权利要求撰写（分步 I/O · 衔接闭合 · 路径下放 · 扩围）

## 适用场景

- 撰写「一种××方法」类**方法独立权利要求**（含算法/数据处理流程型）。
- 审核方法独权是否：步骤切分不清、I/O 不明、步内输入空挂、步间接不上、中间路径过细或假宽。
- 判断某段「输入→输出」的具体计算路径应留独权还是下从权。
- **不替代** `独权撰写`；**不替代** `结构类权利要求撰写`。

**典型触发：** 「写方法独权」「按输入输出重写步骤」「这一步是不是写太细」「步骤衔接不紧」。

---

## 保护客体（为何这样写）

方法发明保护的是：**为解决技术问题而采用的操作流程/处理步骤及其逻辑关系**。

- 侵权比对：是否实质实施同一条 **输入 →（必要变换）→ 输出** 流水线。
- 独权对准**分步流程契约**，不是某次实现的全部中间工序。
- **省去步内中间路径**（下从权）：避免把客体收成「一种具体算法实现」、换等价实现即规避；与扩围一致。
- **不能省的**：删掉后技术问题无解或失去区别的变换准则（不变量）。

须落成技术方案；禁止纯数学兴趣或「智能优化」类空词。

---

## 高质量方法独权总纲（硬性）

> **先把方案清楚拆成若干步；每步明确抽象出输入与输出；步内输入用尽、步间输出咬合；输入到输出的具体路径默认进从权。**

---

## 核心规则

### 规则一：先拆步，再写每步 I/O

1. 先把整体方案拆成**边界清楚**的主步骤（一步一个处理单元），禁止一大段连写导致无法核对 I/O。  
2. 每一步写成可核对契约：

```text
本步（输入）→ 输出
```

常用句式方向：`获取…` / `基于…` / `根据…` → `得到…`（或训练/识别/输出等可核对动词）。  
3. 代理人应能列出主步骤的「输入→输出」对照表（可笔记，交付时口头可复述）。

### 规则二：步内衔接——输入用尽（禁止空挂）

本步**写明的每一项输入**，都必须参与得到**本步输出**。

| 不合格 | 合格处置 |
|--------|----------|
| 「获取 A 和 B」，其后运算只用 A，B 留到后续步骤才用 | 将 B 的获取挪到真正使用它的步骤；或改写本步输出使 B 成为必要输入 |
| 本步输出与所获取输入对不上 | 重定本步 I/O，使「基于这些输入得到该输出」成立 |

**禁止**为凑「获取」清单而空挂输入（典型：与本步训练/抽特征无关的集合与水力模型捆在同一获取句）。

### 规则三：步间衔接——输出咬合输入

- 上一步输出名称与下一步输入对齐（数据血统闭合）。  
- 配合 `预设值与参数来源检查`：参数先获取/算得/标明预设再使用。  
- 集合可空等入口若交底支持，宜在说明书/从权交代；独权勿写别扭的「允许为空」注释句，也不要因此空挂输入。

### 规则四：路径 / 不变量二分（硬判问 · 先判再放）

对拟写入独权的每一段「中间处理」，先三分，再决定去留。**禁止**未经二分就把整段训练/仿真细节塞进独权，也禁止为扩围删到无法实施。

#### 4.1 三问硬判（按序；命中即归类）

| 顺序 | 判问 | 若「是」→ 归类 | 独权处置 |
|------|------|----------------|----------|
| ① | 换成另一种本领域常见等价实现后，**本步声明的输入与输出是否仍成立**？ | **路径（实现层）** | **默认下从权/说明书**；独权不写该具体实现 |
| ② | 不写清该段，本领域技术人员能否仍由本步输入**得到**本步输出（可实施）？ | 若「否」→ 尚不够抽象 | 独权至少保留**概括层**手段（见 4.2）；细节仍从权 |
| ③ | 该段是否正是区别于现有技术、解决锚定技术问题所靠的**变换准则/控制逻辑**？ | **不变量** | **必须留独权**；不得当路径删掉 |

说明：①与③可同时观察——「用余弦」常为路径，「按特征相似度聚类」「在盲区内按误差减少量选」常为不变量。②是扩围刹车：①判为路径后，若独权只剩「基于 A 得到 B」而无人能实施，必须加概括层，不能删光。

#### 4.2 可实施下限（与扩围的平衡）

- 独权每步在压缩路径后，仍须使本领域技术人员**能够实现**该步 I/O。  
- **概括层示例（可留独权）：**「通过仿真得到训练数据并训练得到定位模型，再提取节点特征」——不钉死软件名、层号、维数、损失。  
- **细节层示例（进从权）：** EPANET、漏损面积范围、TCN、最后一卷积层、128 维、余弦、谱聚类、学习率。  
- **禁止：** 为扩围写成无可核对手段的「基于 A 得到 B」空壳；**禁止**把③类不变量误判为路径下放。

#### 4.3 与三层分离的对应

| 层 | 内容 | 位置 |
|----|------|------|
| **I/O 层** | 分步、输入、输出、步内用尽、步间咬合 | **独权** |
| **不变量层** | 判问③为是的准则/控制流 | **独权必须保留** |
| **实现层（路径）** | 判问①为是的算法/工具/参数等 | **从权/说明书** |
| **概括层（若需要）** | 判问②要求的、不钉死实现的必要手段概括 | **独权可保留** |

客体理由：独权锁流程契约与必要准则；具体路径写下独权则范围缩成该实现。

### 规则五：不变量才在独权展开「怎么变」

- 展开对象 = 规则四判为**不变量**的准则（如「在信息盲区内选」「按定位误差减少量选」「加点后重评」），不是源码路径。  
- **「其中」：** 只定义自定义名词的**语义**，不塞完整伪代码路径。

### 规则六：控制流接口化

条件句用 `条件判断语句规范`。独权只留必要分支（切换域、终止等）；细阈值进从权，写「预设的××条件」即可。

### 规则七：扩围手法（去钉死；须先完成规则四）

先规则四二分，再对本步留下的表述去钉死：

| 手法 | 动作 | 边界 |
|------|------|------|
| 去模型/算法/工具/参数/场景钉死 | 上位为完成该 I/O 的类名或预设条件 | 不得跌破 4.2 可实施下限 |
| 术语与问题同级 | 问题已上位则题名与步骤用词同步 | 避免问题宽、独权窄 |
| 成组下放路径 | ①类实现细节成组进从权 | **禁止**把③类不变量整组下放 |

### 规则八：交稿自检

1. 步骤是否拆清？每步能否一句话说清输入与输出？  
2. 步内是否输入用尽、无空挂？步间是否咬合？  
3. 中间处理是否已经规则四三问归类？路径是否下从权？不变量是否仍在？需要时是否有概括层？  
4. 换等价实现是否仍想卡住？想则独权勿钉死该实现。  
5. 自定义词能否仅靠「其中」读懂？有无假宽空词？有无「基于 A 得到 B」却无法实施？

---

## 与独权三版策略的配合

由 `独权撰写` 挂接：

| 版本 | 本 skill |
|------|----------|
| **第一版** | 拆步 + I/O 可追踪；允许暂留部分路径 |
| **第二版** | 步内用尽；规则四二分后下放路径、保留不变量/必要概括层；并行 `独立权利要求必要技术特征检查` |
| **第三版** | 规则四之后再去钉死 + 跑规则八；不得抽空区别骨架、不得跌破可实施下限 |

术语命名仍先走 `部件命名优化总控`。正反例见同目录 `examples.md`（按需 Read）。

---

## 配套调用

| 需求 | 调用 |
|------|------|
| 条件句 | `条件判断语句规范` |
| 参数来源 | `预设值与参数来源检查` |
| 自定义名词 | `自定义名词就地定义` |
| 主谓宾/指代 | `权利要求主谓宾结构规范` / `权利要求指代与参照清晰性规范` |
| 是否非必要 | `独立权利要求必要技术特征检查` |
| 命名扩围 | `部件命名优化总控` |
| 三版/变劣 | `独权撰写` |

---

## 最低交付

1. 主步骤「输入→输出」对照可复述；步内无空挂、步间咬合。  
2. 中间处理已经规则四归类；路径下从权；不变量仍在；需要时有概括层（第一版可暂留部分路径）。  
3. 「其中」定义语义；条件与预设符合配套 skill。  
4. 扩围后仍可实施，最小区别骨架仍在。

## 检查清单

- [ ] 已按客体理解为「流程」而非「实现清单」  
- [ ] 已拆步；每步 I/O 明确；步内用尽、步间咬合  
- [ ] 规则四三问已做；路径/不变量/概括层归类正确  
- [ ] 未跌破可实施下限；未把不变量当路径删  
- [ ] 无假宽；去钉死与问题层级一致  
- [ ] 未误用结构类「先总列后展开」

## 口诀

**先拆步，定 I/O；步内用尽，步间咬合；  
三问归类，路径从权；概括保底，准则留骨。**

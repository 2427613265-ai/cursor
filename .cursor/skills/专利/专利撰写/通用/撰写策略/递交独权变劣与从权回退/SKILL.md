---
name: 递交独权变劣与从权回退
description: 【已并入独权撰写】原独立 skill 已并入 `独权撰写` 第8步（内嵌子步骤 8.1～8.4）。需要「定稿再放宽作递交权1、改进进从权」时，直接使用 `独权撰写`，勿再按本文件旧四步流程执行。
metadata:
  中文名称: 递交独权变劣与从权回退（已并入独权撰写）
  所属分支: 专利/专利撰写/通用/撰写策略
---

# 【已并入】递交独权变劣与从权回退

本文件**已退役为跳转说明**。

完整规则与分步交付要求见：

**`专利/专利撰写/通用/撰写策略/独权撰写`** → **第8步【可选】递交独权变劣与从权回退（本 skill 内嵌）**（子步骤 8.1～8.4）

位置：第三版独权之后、第9步收束之前。

若用户触发「变劣递交」「定稿进从权」等，应打开并执行 `独权撰写` 第8步，**禁止**再按本目录旧版四步独立跑一遍。

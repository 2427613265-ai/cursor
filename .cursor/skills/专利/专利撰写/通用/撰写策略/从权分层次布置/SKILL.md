---
name: 从权分层次布置
description: 从权第2/3步用：关键创新点逐层落实；每条从权都是独立技术方案，须假想代入被引用权全文。由从权撰写调用。不写次要独权组。
metadata:
  中文名称: 从权分层次布置
  所属分支: 专利/专利撰写/通用/撰写策略
---

# 从权分层次布置（核心权利要求组的从权）

`从权撰写` **第2步布局、第3步逐条写**必读。正反例见同目录 `examples.md`。只管**核心独立权利要求**下面的从权怎么分层、怎么想；门禁、形式、依附编号仍走 `从权撰写`。次要独权组见 `次要权利要求组撰写`。

从权撰写复杂，本页只抓两个重点。其余注意点仍用从权撰写已有门禁，不在此另开长文。

---

## 两个重点（硬性）

### 1. 关键创新点是从权的重点，须逐层落实

独权第1步的**关键创新点**（立地，见 `核心区别技术特征确定`）默认作从权保底，是本核心组从权的**主骨架**，应当通过从权**逐层**写进去，而不是一条从权塞完、也不是只拿边角下放组铺满 9 条却把关键创新点晾在一边。

**逐层：** 先写较宽的一层（权2常接关键创新点的主干），再在其下加更具体的一层（结构细部、参数、路径、优选配合）。独权阶段「宜下放 / 未入最小集 / 第二版剔出」的种子，按所附属的层次挂到对应从权，不要打乱层次去平行堆。

**不是：** 把彼此独立的改进点硬串联成一条链冒充分层。独立改进仍须**平行**引同一合适基础（多为权1）。平行/串联判定见 `权利要求引用结构检查`；本页管「关键创新点要分层占位」，不替代该检查。

### 2. 每一条从权都是一个独立的技术方案

引用某一条权利要求只是**简化写法**。思考时、组织语言时，都应当把被引用权利要求（及其引用链一直到权1）的内容**以假想方式完全代入**，再展开本条要加的特征。

写完后在心里把引用链拼成一份完整方案：须仍能独立实施，本条新特征接得上、不缺前提、不与已有特征矛盾。句子可以只写「根据权利要求X……，其特征在于……」，但不得把本条当成悬空片段来想。

---

## 布局时怎么用（第2步）

1. 在布局表标出：哪几条从权落实关键创新点的第几层（主干 / 再具体 / 再优选）。  
2. 关键创新点占位**优先于**边角诱鱼、纯说明书细部。独权下放组仍强制入库；与关键创新点冲突时，先保分层骨架，其余合并或进暂缓。  
3. 同一关键创新点的加细 → 串联；与之无关的独立改进 → 平行引权1（或同一合适基础）。  
4. 默认仍 9 条（权2～权10）；层次不够用则加条须改表再确认，不得把两层无关的加细塞进同一条从权充数。

## 逐条写时怎么用（第3步）

写权 N 前：用一两句复述「代入后的完整方案是什么」（可极短，但须真代入）。再写本条新增特征。交付时勾：「已假想代入被引用权全文」。

**过窄：** 关键创新点整份堆在权2，后面从权与保底无关；或该平行的独立改进被串进关键创新点链，保护被绑死。  
**过残：** 只写引用号、不代入，本条特征与权1/被引权接不上或矛盾。

## 禁止

- 用边角特征占满 9 条，关键创新点未逐层落实。  
- 以「引用了权X」代替假想代入。  
- 把独立改进硬串联冒充分层。  
- 同批写次要独立权利要求组。
